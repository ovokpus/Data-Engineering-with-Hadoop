# Spark Basic Actions Part II Example Project

Project demonstrates usage of a second set of Basic Actions (reduce, min, max, sum and mean) in Spark Java Library.

To run execute:

`mvn install exec:exec`
