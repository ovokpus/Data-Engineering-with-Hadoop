# Spark Join Example Project

Project demonstrates usage of the `join` transformations with the classic variants:

- Inner Join
- Left Outer Join
- Right Outer Join
- Full Outer Join

To run execute:

`mvn install exec:exec`
