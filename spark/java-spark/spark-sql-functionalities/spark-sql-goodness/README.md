# Spark SQL Introductory Example Project

Project demonstrates usage of the `SparkSQL` focusing on declaring a view and running simple queries against it, within the *Spark* API.

To run execute:

`mvn install exec:exec`
